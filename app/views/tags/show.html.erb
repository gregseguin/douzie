<h2><span>Funny <%= @tag.name.titlecase %> Jokes</span></h2>

  <ol class="circle-list">
      <% @tag.jokes.each do |joke| %>
              <%= link_to joke_path(joke) do %>
              <li>
                <h2><%= joke.title %></h2>
                <p><%= strip_tags smart_truncate(joke.saying, :setences => 1) %></p>
              </li>
              <% end %>
      <% end %>
  </ol>


